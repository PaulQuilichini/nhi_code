name = "agent"
description = "Full coding agent with workspace write access"

[policy]
sandbox = "workspace-write"
approval = "on-request"
denied_tools = []

[prompt]
system_addendum = """
You are in Agent mode. You can read and modify files in the workspace, run shell commands, and spawn sub-agents when needed. Always explain what you're doing before making significant changes.
"""
